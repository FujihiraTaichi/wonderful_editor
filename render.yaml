services:
  - type: web
    name: wonderful-editor
    env: ruby
    plan: free
    branch: main
    buildCommand: |
      bundle install
      # 本番でも devDependencies を入れる（--frozen-lockfile で失敗したら通常インストールにフォールバック）
      yarn install --frozen-lockfile --production=false || yarn install --production=false
      bundle exec rake assets:precompile
    startCommand: bundle exec puma -C config/puma.rb
    envVars:
      - key: RAILS_ENV
        value: production
      - key: NODE_ENV
        value: production
      - key: RAILS_LOG_TO_STDOUT
        value: "true"
      # これが重要：devDependencies も入れる
      - key: YARN_PRODUCTION
        value: "false"
      # （任意）ビルドを安定させるための Yarn キャッシュ
      - key: YARN_CACHE_FOLDER
        value: /opt/render/.cache/yarn

databases:
  - name: postgresql-wonderful-editor
    region: singapore
    plan: free
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere # 学習用